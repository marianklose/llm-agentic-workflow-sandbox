"""
LangGraph workflow construction.
A LangGraph is basically a flowchart with decision points.
We have the different nodes (= functions) and the 
edges between them (= control flow, i.e. which node runs after which).
"""